alias ExAdmin.Utils
defprotocol ExAdmin.Theme do
  # @fallback_to_any true
  # def get_form_error_class(theme, error) 
  # def wrap_item_type(theme, type, label, ext_name, contents, error) 
  # def build_form_error(theme, error)
end

